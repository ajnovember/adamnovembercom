var last = null;
	

	function mySlide(element) {
		
		//var x = element.getElementsByClassName('pnav_pop');
		//$(x[0]).slideDown();
		//if(last!=x){$(last).slideUp();}
		//console.log(x);

		last = x;
	}
